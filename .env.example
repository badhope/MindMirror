# MindMirror (static / GitHub Pages edition)
# Copy this file to .env.local if you want to override anything.
# Vite only reads .env / .env.local / .env.[mode].

# Repository URL used in the About page when linking to the user guide.
# Defaults to the GitHub repo when not set.
VITE_REPO_URL=
