version: '3.8'

name: mindmirror

services:
  # Frontend - React + Nginx
  client:
    build:
      context: ../
      dockerfile: deploy/client/Dockerfile
    container_name: mindmirror-client
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - server
    networks:
      - mindmirror-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Backend - FastAPI + Python
  server:
    build:
      context: ../backend
      dockerfile: ../deploy/server/Dockerfile
    container_name: mindmirror-server
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      - DATABASE_URL=sqlite:///data/mindmirror.db
      - ENVIRONMENT=production
    volumes:
      - mindmirror-data:/app/data
    networks:
      - mindmirror-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Optional: Redis for caching
  redis:
    image: redis:7-alpine
    container_name: mindmirror-redis
    restart: unless-stopped
    expose:
      - "6379"
    volumes:
      - redis-data:/data
    networks:
      - mindmirror-network
    command: redis-server --appendonly yes
    profiles:
      - with-redis

networks:
  mindmirror-network:
    driver: bridge

volumes:
  mindmirror-data:
    driver: local
  redis-data:
    driver: local
